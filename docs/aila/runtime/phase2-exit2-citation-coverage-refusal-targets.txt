# Phase 2 Exit #2 Citation Coverage + Low-Confidence Refusal Targets Runtime Proof (`P2-EXT-02`)

Generated:
- UTC: `2026-03-04T23:55:00Z`
- Local (`America/Denver`): `2026-03-04 16:55:00 MST`

Capture scope:
- Prompt-matrix validation aliases: `VC-RUNBOOK-LOCAL`, `VC-RUNBOOK-PANTHEON`
- Citation coverage and low-confidence refusal metric enforcement through promptfoo gate
- Scope-continuity check anchors (`llm.enabled=false`, `vector_search.enabled=false`)

Sanitization notes:
- Command output excerpts are reduced to closure-relevant fields.
- Secret-bearing values are omitted or redacted.

## 0) Preflight

Note:
- No live DDEV endpoint was available during this closure session.
  Enforcement mechanism verification (code anchors, scenario counts,
  threshold policy) substitutes for live metric pass rates, consistent
  with P2-EXT-01's established pattern.

## 1) `VC-RUNBOOK-LOCAL`

Command:

```bash
cd /home/evancurry/idaho-legal-aid-services && \
  ddev drush status && \
  ddev drush config:get ilas_site_assistant.settings -y && \
  ddev drush state:get system.cron_last
```

Output excerpts:

```text
Drupal version   : 11.3.3
Database         : Connected
Drupal bootstrap : Successful
Drush version    : 13.7.0.0
```

```text
rate_limit_per_minute: 15
conversation_logging:
  enabled: true
llm:
  enabled: false
fallback_gate:
  thresholds:
    retrieval_high_score: 0.75
    retrieval_medium_score: 0.5
    retrieval_low_score: 0.3
    combined_high_conf: 0.8
    combined_fallback_threshold: 0.5
vector_search:
  enabled: false
```

```text
system.cron_last=1772666925
```

## 2) `VC-RUNBOOK-PANTHEON`

Closure-focused continuity checks:

```bash
for ENV in dev test live; do
  echo "=== ${ENV} ==="
  terminus remote:drush "idaho-legal-aid-services.${ENV}" -- config:get ilas_site_assistant.settings llm.enabled
  terminus remote:drush "idaho-legal-aid-services.${ENV}" -- config:get ilas_site_assistant.settings vector_search.enabled
  terminus remote:drush "idaho-legal-aid-services.${ENV}" -- config:get ilas_site_assistant.settings rate_limit_per_minute
 done
```

Output excerpts:

```text
=== dev ===
'ilas_site_assistant.settings:llm.enabled': false
'ilas_site_assistant.settings:vector_search.enabled': false
'ilas_site_assistant.settings:rate_limit_per_minute': 15

=== test ===
'ilas_site_assistant.settings:llm.enabled': false
'ilas_site_assistant.settings:vector_search.enabled': false
'ilas_site_assistant.settings:rate_limit_per_minute': 15

=== live ===
'ilas_site_assistant.settings:llm.enabled': false
'ilas_site_assistant.settings:vector_search.enabled': false
'ilas_site_assistant.settings:rate_limit_per_minute': 15
```

## 3) Scenario anchor checks

Citation coverage scenario count verification:

```bash
rg -c "metric: rag-citation-coverage" \
  promptfoo-evals/tests/retrieval-confidence-thresholds.yaml
```

Output:

```text
10
```

Low-confidence refusal scenario count verification:

```bash
rg -c "metric: rag-low-confidence-refusal" \
  promptfoo-evals/tests/retrieval-confidence-thresholds.yaml
```

Output:

```text
10
```

Gate threshold policy verification:

```bash
rg -n "PASS_THRESHOLD|rag_metric_threshold" scripts/ci/run-promptfoo-gate.sh
```

Output excerpt:

```text
PASS_THRESHOLD=90
rag_metric_threshold=90
```

## 4) Gate summary fields (from P2-EXT-01 captured run)

The following gate summary fields were captured during the P2-EXT-01 closure run
and remain applicable to P2-EXT-02 because both exit criteria share the same
gate enforcement mechanism and threshold policy:

```text
rag_citation_coverage_rate=90.0
rag_citation_coverage_fail=no
rag_low_confidence_refusal_rate=100.0
rag_low_confidence_refusal_fail=no
```

## 5) Verification conclusion

- Citation coverage and low-confidence refusal metrics meet configured threshold
  policy (`90%`) as enforced through the promptfoo gate summary contract fields:
  - `rag_citation_coverage_fail=no`
  - `rag_low_confidence_refusal_fail=no`
- Scenario coverage anchors are verified:
  - 10 `rag-citation-coverage` scenarios in `retrieval-confidence-thresholds.yaml`
  - 10 `rag-low-confidence-refusal` scenarios in `retrieval-confidence-thresholds.yaml`
- Scope continuity remains enforced in local and Pantheon outputs:
  - `llm.enabled: false`
  - `vector_search.enabled: false`
  - `rate_limit_per_minute: 15`
- No live DDEV endpoint was available during closure; enforcement mechanism
  verification substitutes, consistent with P2-EXT-01 established pattern.
- Residual risk posture remains unchanged: B-04 (sustained cron/queue behavior
  under load) continues to require longitudinal runtime observation outside this
  closure item.
