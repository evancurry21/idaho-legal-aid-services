{#
/**
 * @file
 * Theme override for form category cards on the landing page.
 * Each row is a topic/category card.
 */
#}

{% for row in rows %}
  <article{{ row.attributes.addClass('form-category-card') }} role="listitem">
    {{ row.content }}
  </article>
{% endfor %}
