---
phase: 03.1
plan: 01
subsystem: ci-test-infrastructure
tags: [bash, ci, gates, lockfile, bats, test-infrastructure, wave-0]
requirements_completed: [PIPE-05, PIPE-07, PIPE-09]
dependency_graph:
  requires: []
  provides:
    - scripts/ci/gates.lock.json (consumed by Wave 3 plan 03.1-05 parity verifier)
    - tests/ci/_assert.sh (sourced by all CI test harnesses in waves 1-4)
    - tests/ci/sigpipe-repro.sh (invoked by Wave 1 spike chain — REAL harness, not stub)
    - tests/ci/four-path-matrix.sh (wired by Wave 4 plan 03.1-07)
    - tests/ci/dual-remote-invariant.sh (wired by Wave 3 plan 03.1-06)
    - tests/ci/gates-parity.bats (wired by Wave 3 plan 03.1-05)
  affects:
    - .planning/phases/03.1-publish-pipeline-audit-hardening/03.1-VALIDATION.md (append-only tooling audit)
tech_stack:
  added: []
  patterns:
    - canonical-shell-header (set -euo pipefail + SCRIPT_DIR/REPO_ROOT)
    - sourceable-library-guard (ILAS_TESTS_ASSERT_LIB_SOURCED idempotent pattern)
    - lockfile-based-parity (JSON data file; verifier added in Wave 3)
    - hand-rolled-bash-fallback (bats not installed; helpers in _assert.sh)
key_files:
  created:
    - scripts/ci/gates.lock.json
    - tests/ci/_assert.sh
    - tests/ci/sigpipe-repro.sh
    - tests/ci/four-path-matrix.sh
    - tests/ci/dual-remote-invariant.sh
    - tests/ci/gates-parity.bats
    - .planning/test-bed/ (directory + local .gitignore + .gitkeep; not tracked)
  modified:
    - .planning/phases/03.1-publish-pipeline-audit-hardening/03.1-VALIDATION.md (append-only tooling audit row)
decisions:
  - "Hand-rolled bash fallback for bats-core (non-interactive sudo unavailable; per RESEARCH §A5)"
  - "Shellcheck-clean by construction (manual review fallback); operator install path documented"
  - "JSON lockfile (not YAML) — jq is universally available, yq is not (per RESEARCH §Recommendation)"
  - "Lockfile encodes Wave-2 target name gate_promptfoo_deploy_bound_required (currently missing in publish-gates.lib.sh) — drives PIPE-02 contract enforcement when parity verifier lands"
metrics:
  duration: ~10 minutes (single executor pass)
  task_count: 5
  file_count: 7 (6 created + 1 modified)
  commit_count: 4 (Tasks 1-4) + 1 (this SUMMARY) = 5 commits total
  completed_date: 2026-05-11
---

# Phase 03.1 Plan 01: Wave 0 Test Infrastructure Summary

**One-liner:** Established the Wave 0 test bed for the publish pipeline audit/hardening phase — canonical gate manifest (`scripts/ci/gates.lock.json`), sourceable assertion library (`tests/ci/_assert.sh`), four CI test harness scaffolds, and a real (non-stub) SIGPIPE FD-isolation spike — without touching any production pipeline script or `.git/hooks/` file.

## Outcomes

| Artifact | Status | Purpose |
|----------|--------|---------|
| `scripts/ci/gates.lock.json` | ✅ created (5 gates, valid JSON) | Canonical manifest consumed by Wave 3 plan 03.1-05 parity verifier (PIPE-05). |
| `tests/ci/_assert.sh` | ✅ created (4 assert helpers, idempotent guard) | Shared sourceable library for all wave-1+ CI harnesses; never sourced by production code (PIPE-08). |
| `tests/ci/sigpipe-repro.sh` | ✅ created (REAL, not stub; 9 `</dev/null` occurrences) | Wave 1 FD-isolation spike (RESEARCH Hypothesis #1) — emits verdict token (hypothesis1-confirmed/falsified/inconclusive). |
| `tests/ci/four-path-matrix.sh` | ✅ created (stub, exits 64) | Future PIPE-07 driver; argparse stub mirrors `scripts/git/sync-check.sh:23-54`. |
| `tests/ci/dual-remote-invariant.sh` | ✅ created (stub, exits 64) | Future PIPE-06 typed-drift invariant. |
| `tests/ci/gates-parity.bats` | ✅ created (hybrid bats/bash; fail-loud stub) | Future PIPE-05 parity check; works under bats AND under bash. |
| `.planning/test-bed/` | ✅ created with `.gitignore` and `.gitkeep` (not tracked) | Sentinel directory for Wave 4 result/sentinel files. Local-only; `.planning/` is gitignored project-wide. |
| `03.1-VALIDATION.md` | ✅ appended | Recorded tooling audit (jq present; bats/shellcheck missing → fallback) under new `## Tooling Audit (Wave 0 Result)` heading. |

## Five Canonical Gate IDs (in `scripts/ci/gates.lock.json`)

| `id` | `library_fn` | `required_in` | Status |
|------|--------------|---------------|--------|
| `composer_dryrun` | `gate_composer_dryrun` | `pre-push`, `publish-gate-local`, `ci:quality-gate` | function exists in publish-gates.lib.sh |
| `vc_pure` | `gate_vc_pure` | `pre-push`, `publish-gate-local`, `ci:quality-gate` | function exists |
| `module_quality` | `gate_module_quality` | `pre-push`, `publish-gate-local`, `ci:quality-gate` | function exists (11-min pole; FD-leak suspect) |
| `promptfoo_branch_aware` | `gate_promptfoo_branch_aware` | `pre-push`, `publish-gate-local` | function exists (advisory; gated by `ILAS_LIVE_PROVIDER_GATE`) |
| `promptfoo_deploy_bound` | `gate_promptfoo_deploy_bound_required` | `pre-push:master-origin-only` | **target name** — function does not yet exist; Wave 2 plan 04 creates it (PIPE-02 fail-closed replacement for current silent-skip behavior). |

## Four Stub Harness Exit Contracts

| File | Stub exit | Stderr message | Wire-up wave |
|------|-----------|----------------|--------------|
| `tests/ci/four-path-matrix.sh` | `64 EX_USAGE` | `[FAIL] four-path-matrix not yet wired — Wave 4 plan 07 implements` | Wave 4 |
| `tests/ci/dual-remote-invariant.sh` | `64 EX_USAGE` | `[FAIL] dual-remote-invariant not yet wired — Wave 3 plan 06 implements` | Wave 3 |
| `tests/ci/gates-parity.bats` (bash dispatch) | `64 EX_USAGE` | `[FAIL] gates-parity stub — Wave 3 plan 05 wires the real verifier; bats not installed` | Wave 3 |
| `tests/ci/sigpipe-repro.sh` | NOT A STUB — `0` if harness ran, `1` only on script-side failure | verdict token (`hypothesis1-confirmed` / `hypothesis1-falsified` / `inconclusive`) on final stdout line | Wave 1 (invoked) |

## Tooling Install Path

| Tool | Wave 0 result | Chosen path |
|------|---------------|-------------|
| `jq` | already on PATH (jq-1.7) | use as-is |
| `bats-core` | not installed; sudo apt-get requires interactive auth | **hand-rolled bash fallback** via `tests/ci/_assert.sh`; bats remains valid if/when installed (no code change needed) |
| `shellcheck` | not installed; same sudo constraint | **manual review** + shellcheck-clean-by-construction (canonical header, double-quoted vars, `[[ ]]` tests). Operator install: `sudo apt-get install -y shellcheck`. |

## Per-Task Verification (results)

```
$ jq -e '.version == 1 and (.gates | length == 5)' scripts/ci/gates.lock.json
true
$ bash -c 'source tests/ci/_assert.sh; assert_eq x x integration-source'
[ok] integration-source
$ bash tests/ci/four-path-matrix.sh; echo $?
[FAIL] four-path-matrix not yet wired — Wave 4 plan 07 implements
64
$ bash tests/ci/dual-remote-invariant.sh; echo $?
[FAIL] dual-remote-invariant not yet wired — Wave 3 plan 06 implements
64
$ bash tests/ci/gates-parity.bats; echo $?
[FAIL] gates-parity stub — Wave 3 plan 05 wires the real verifier; bats not installed
64
$ grep -c '</dev/null' tests/ci/sigpipe-repro.sh
9                         # ≥ 4 required (acceptance criteria)
$ grep -rE 'tests/ci/_assert\.sh' scripts/ci/ scripts/git/ web/ | wc -l
0                         # PIPE-08 anti-centralization satisfied
$ git diff --name-only $(git merge-base HEAD master)..HEAD | grep -E '^scripts/(ci|git)/.*\.sh$'
                          # (empty) — no production .sh modified
```

## Plan Acceptance Mapping

| Plan success criterion | Result |
|------------------------|--------|
| `bats`, `shellcheck`, `jq` on PATH (or fallback documented) | jq on PATH; bats/shellcheck fallback documented in VALIDATION.md §Tooling Audit |
| `scripts/ci/gates.lock.json` valid JSON, 5 gates, required schema | ✅ verified by `jq -e` |
| `tests/ci/_assert.sh` sourceable, 4 assertions, idempotent | ✅ verified (4 functions via `declare -F`; double-source no-op) |
| `tests/ci/four-path-matrix.sh`, `sigpipe-repro.sh`, `dual-remote-invariant.sh` exist + executable | ✅ verified (exit 64 / verdict-emitting respectively) |
| `tests/ci/sigpipe-repro.sh` REAL FD-isolation (≥ 4 `</dev/null`) | ✅ 9 occurrences |
| `tests/ci/gates-parity.bats` exists as stub | ✅ |
| `.planning/test-bed/` with `.gitignore` for `results-*.txt` and `sentinel.txt` | ✅ created locally; not tracked per plan Task 5 explicit direction |
| No production script references new file | ✅ `grep` returns 0 |
| Phase verifier confirms Wave 0 acceptance | (to be confirmed by /gsd-verify-phase in orchestrator path) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 — Missing critical config] Hand-rolled bash fallback for bats-core**
- **Found during:** Task 1
- **Issue:** Plan acceptance criteria mandate `bats` and `shellcheck` on PATH or "fallback documented." Neither is installed on the host; non-interactive sudo is unavailable, so `apt-get install` cannot proceed without operator action.
- **Fix:** Per RESEARCH §A5 / §"Environment Availability" the hand-rolled fallback is acceptable. Recorded the audit + fallback rationale + operator install command under a new `## Tooling Audit (Wave 0 Result)` section of `03.1-VALIDATION.md`. The `.bats` harness uses a hybrid file that runs under bats (when installed) AND under bash via a dispatcher.
- **Files modified:** `.planning/phases/03.1-publish-pipeline-audit-hardening/03.1-VALIDATION.md`, `tests/ci/gates-parity.bats`
- **Commits:** 9d316c18a, 86dbc947c

**2. [Rule 3 — Blocking issue] Worktree HEAD reset to base commit**
- **Found during:** Pre-task worktree branch check
- **Issue:** Initial `git merge-base` returned non-zero — the worktree branch was based on `d32910ed8` ("Apply donation branch changes to main") which has no common ancestor with the required base `69abaeda9`. Per the executor protocol the worktree must be based on `69abaeda9`.
- **Fix:** Ran `git reset --hard 69abaeda9d67199f386bde36ddf0c210c9f3ae71` per the worktree_branch_check block.
- **Commits:** none directly; precondition for all subsequent commits.

### Plan-Literal Choices

- **Task 5 — no commit for `.planning/test-bed/`:** The plan explicitly states "do NOT attempt to `git add` them (gitignored)." Followed literally — the directory + `.gitignore` + `.gitkeep` exist on the local filesystem but are not in any commit. Verified `git status --ignored .planning/test-bed/` shows the directory as ignored.
- **`.planning/phases/03.1-publish-pipeline-audit-hardening/03.1-VALIDATION.md` force-added with `git add -f`:** `.planning/` is gitignored repo-wide. Task 1 nonetheless instructs editing VALIDATION.md, and the file's tooling audit row is needed by future executors. Same convention is applied to this SUMMARY.md. This is consistent with how the GSD workflow treats phase-scoped planning artifacts.

### Auth Gates

None encountered.

## Known Stubs

| File | Reason | Resolves in |
|------|--------|-------------|
| `tests/ci/four-path-matrix.sh` | Driver TBD; needs Wave 4 sentinel pattern + four-path argparse | Wave 4 plan 03.1-07 |
| `tests/ci/dual-remote-invariant.sh` | Driver TBD; extends `describe_remote_status` in `scripts/git/common.sh:89-116` | Wave 3 plan 03.1-06 |
| `tests/ci/gates-parity.bats` | Verifier TBD; needs `scripts/ci/verify-gates-parity.sh` (Wave 3 plan 03.1-05) | Wave 3 plan 03.1-05 |
| `gates.lock.json` entry `gate_promptfoo_deploy_bound_required` | Function name encoded but not yet defined in `scripts/ci/publish-gates.lib.sh` | Wave 2 plan 03.1-04 (PIPE-02) |

All stubs are **intentional** per the plan; each is documented with its wire-up wave and fail-loud (exit 64) until then. No stub blocks the Wave 0 goal.

## Threat Flags

None introduced. The threat register entries (T-03.1-W0-01 through 05) are all `mitigate`/`accept` and are addressed by Task 4 acceptance criteria (stubs exit 64 fail-loud) and the PIPE-08 grep-check (no production import of `tests/ci/_assert.sh`).

## Commits

| # | Hash | Message |
|---|------|---------|
| 1 | `9d316c18a` | `test(03.1-01): record Wave 0 tooling audit in VALIDATION.md` |
| 2 | `877d7476a` | `feat(03.1-01): add canonical gate manifest scripts/ci/gates.lock.json` |
| 3 | `bf0255701` | `test(03.1-01): add tests/ci/_assert.sh sourceable assertion library` |
| 4 | `86dbc947c` | `test(03.1-01): add 4 CI test harness scaffolds for PIPE-05/06/07 + SIGPIPE spike` |
| 5 | (this commit) | `docs(03.1-01): complete Wave 0 test infrastructure plan` |

## Self-Check: PASSED

Verified at 2026-05-11:
- 10 of 10 claimed files/directories exist on disk (6 newly created, 1 modified, 2 test-bed metadata, 1 SUMMARY).
- 4 of 4 task commits resolvable in `git log` (hashes 9d316c18a, 877d7476a, bf0255701, 86dbc947c).
- 5th commit (this SUMMARY) added immediately after this section is written.
