---
phase: 03.1
plan: 02
type: spike-evidence
wave: 1
executed_at: 2026-05-11T20:17:59Z
executed_in_worktree: agent-a6932865f5ec4282c
worktree_branch: worktree-agent-a6932865f5ec4282c
push_mode: dry-run-only
operator_decision_locked: 2026-05-11
artifact_purpose: |
  Authoritative empirical evidence for SIGPIPE root-cause hypothesis verdicts
  per PIPE-01 / D-SIGPIPE-02. Wave 2 fix plans cite this document by §heading.
---

# Plan 03.1-02 — SIGPIPE Hypothesis Spike

**Executor agent:** parallel worktree `agent-a6932865f5ec4282c`
**Push mode:** `git push --dry-run -v origin master` only (no real pushes; operator-locked).
**Production scripts modified:** zero (`git diff --name-only HEAD scripts/` empty post-spike).
**Hook restored:** verified `diff -q .git/hooks/pre-push scripts/ci/pre-push-strict.sh` exit 0 after every variant; matching sha256 `a8196d23759e3e323e6e3d9a40aab9f55eaf0ac07d59d1a7059955b7fda78137`.

## Introduction

This spike empirically dispositions the four SIGPIPE root-cause hypotheses for
the `git push origin master` SIGPIPE-141 incident
(`.planning/todos/pending/2026-05-07-fix-git-push-origin-master-sigpipe-in-pre-push-hook.md`)
before any production code is edited. Per D-SIGPIPE-01 (spike-first-fix-second),
no Wave 2 fix plan may run until this artifact's `## Recommended Fix Shape` is
approved by the operator.

Operator constraints baked into this run:

1. **Hypothesis #1 push mode:** `git push --dry-run -v origin master` only. No
   real push to master under any circumstance.
2. **Hypothesis #2:** Deferred. The 1200-second SSH idle test cannot reliably be
   run by an executor agent inside a sandboxed bash; the script is staged for
   operator execution.
3. **Hypothesis #4:** Deferred. `sudo tcpdump` is not available without
   interactive root authorization.
4. **No `--no-verify`.** **No `git stash`.** **No `git push` without `--dry-run`.**

### What dry-run CAN and CANNOT prove

Per `git help githooks` (CITED): the `pre-push` hook receives the push refs on
stdin and is invoked identically by `git push` and `git push --dry-run`. So:

- **Dry-run DOES exercise:** every line of `pre-push-strict.sh` (stdin lifecycle
  lines 40-43, sync-check at :145, the gate calls at :199-207, the EXIT trap
  installed at publish-gates.lib.sh:76-78).
- **Dry-run does NOT exercise:** the pack-data send phase that happens AFTER
  hook exit. If the SIGPIPE-141 originates from git writing pack data to an
  SSH pipe whose remote side has died, dry-run cannot observe it.

This caveat is the reason every "CONFIRMED" verdict in this spike is qualified
as `CONFIRMED-DRY-RUN` and the Wave 2 fix plan must close the gap with a
sentinel-commit real-push validation.

## Hypothesis #1 — FD-Inheritance

**Verdict:** **INCONCLUSIVE-INFRASTRUCTURE** (necessary-but-not-sufficient
evidence; the spike could not exercise the highest-suspicion call sites because
the host environment failed an earlier gate).

**Spike script:** `.planning/phases/03.1-publish-pipeline-audit-hardening/scratch/spike-fd-isolation.sh`
**Driver log:** `/tmp/spike-fd-trace-1778530679.log` (size 366 lines; preserved
during this session).

**Patches applied to a copy of `scripts/ci/pre-push-strict.sh`:**

```
+ gate_composer_dryrun </dev/null                                    # was :199
+ gate_vc_pure </dev/null                                            # was :200
+ gate_module_quality </dev/null                                     # was :201
+ gate_promptfoo_deploy_bound "$PROMPTFOO_BRANCH" "$DDEV_ASSISTANT_URL" </dev/null  # was :204
+ gate_promptfoo_branch_aware "$PROMPTFOO_BRANCH" </dev/null         # was :206
+ exec 0<&-                                                          # after :43 PUSH_LINES `done`
```

Patch self-check counters during installation: `</dev/null adds=5 (expect 5);
exec 0<&- adds=1 (expect 1)` — both matched.

**Run:**

```
[spike-fd-isolation] running: git push --dry-run -v origin master
[spike-fd-isolation] log: /tmp/spike-fd-trace-1778530679.log
[spike-fd-isolation] dry-run exit=1
```

**Observed execution path (extracted from the dry-run stderr/stdout):**

```
Pushing to ssh://codeserver.dev.0bbb0799-c3de-441d-8d26-5caed25eba3f.drush.in:2222/~/repository.git
Strict pre-push gate: remote=origin branch=worktree-agent-a6932865f5ec4282c
Checking dual-remote drift before push...
[info] Fetching origin...
   69abaeda9..02ec4f35e  master     -> origin/master
[ok] No remote is ahead of local 'worktree-agent-a6932865f5ec4282c'.
Promptfoo policy target branch: master

=== Gate: Composer install dry-run ===
  ... 326 installs simulated, succeeded ...

=== Gate: VC-PURE (phpunit.pure.xml) ===
publish-gates.lib.sh: line 163: vendor/bin/phpunit: No such file or directory

FAIL: VC-PURE phpunit suite
```

**Evidence points (per acceptance criteria — exit code + stderr signature):**

| Point | Value |
|-------|-------|
| Hook stdin-loop completion | OK — `Promptfoo policy target branch: master` printed AFTER the PUSH_LINES read |
| `exec 0<&-` injection | OK — no breakage; gate output proceeded normally |
| `gate_composer_dryrun </dev/null` | OK — 326-package dry-run completed without SIGPIPE |
| Exit code | 1 (gate-internal failure at vc_pure), NOT 141 (SIGPIPE) |
| stderr signature | `vendor/bin/phpunit: No such file or directory` — environmental, not SIGPIPE |

**Why INCONCLUSIVE (not CONFIRMED-DRY-RUN, not FALSIFIED):**

The patches were applied, but the spike never reached
`gate_module_quality` (the 11-minute gate, RANK 1 likely SIGPIPE site per
RESEARCH §"Hypothesis Ranking") or the promptfoo gate. The worktree's
`vendor/bin/phpunit` is missing — Composer was simulated, not actually
installed — so `gate_vc_pure` exited 1 before the suspect call sites ran. The
patches did not introduce new failures, but they could not be evaluated against
their target.

**What CAN be concluded:**

- The `exec 0<&-` after the PUSH_LINES loop is **safe** (no regression). This
  is a useful Wave 2 input — the structural fix is non-breaking.
- The `</dev/null` on `gate_composer_dryrun` is **safe** (no regression). Same.
- No SIGPIPE-141 was observed in any gate that did run. This is a weak point
  (only short gates ran) but at minimum the short path has no FD-inheritance
  SIGPIPE bug today.

**What CANNOT be concluded:**

- Whether `gate_module_quality` or the promptfoo gate has FD-inheritance
  SIGPIPE behavior. These are the long-runtime gates most likely (per
  RESEARCH §Pitfall 1) to leak FDs through `ddev exec` and `npm run`
  invocations.
- Whether the SIGPIPE originates in the gates at all (vs. in the pack-data
  send phase, which dry-run can't observe).

**Required follow-up for Wave 2 plan 03:**

1. Install composer dev-deps (`composer install`) in the worktree where the
   real-push validation runs, so `vendor/bin/phpunit` is present and
   `gate_vc_pure` passes through to `gate_module_quality`.
2. Run the same FD-isolation patches but with a real push (sentinel commit
   pattern per RESEARCH §"Test Bed Strategy"). The sentinel-commit pattern is
   already planned for Wave 2 plan 03 Task 4 — fold the FD-isolation patches
   into that real-push run and observe whether SIGPIPE-141 reproduces.

## Hypothesis #2 — SSH Idle Timeout

**Verdict:** **DEFERRED — requires operator-driven SSH session.**

**Spike script:** `.planning/phases/03.1-publish-pipeline-audit-hardening/scratch/spike-ssh-idle.sh`
(staged, NOT executed by this agent).

**Why deferred:**

The hypothesis requires opening a real SSH session to
`codeserver.dev.0bbb0799-c3de-441d-8d26-5caed25eba3f.drush.in:2222` and
holding it idle for 1200 seconds (~20 minutes) with client keepalives
explicitly disabled (`-o ServerAliveInterval=0 -o ServerAliveCountMax=0`) to
measure the SERVER's idle timeout. A 20-minute interactive bash invocation
inside this executor's sandboxed harness is not reliable; the operator should
run it from a normal interactive shell.

**Operator instructions:**

```bash
bash .planning/phases/03.1-publish-pipeline-audit-hardening/scratch/spike-ssh-idle.sh \
  2>&1 | tee /tmp/spike-ssh-idle-$(date +%s).log
```

Expected wall-clock: ~20 minutes.

**Verdict interpretation (apply to whatever runs):**

| Elapsed | Behavior observed | Verdict |
|---------|-------------------|---------|
| `>= 1200s` AND `still-alive` printed | SSH held idle for 20 min | **FALSIFIED** — idle timeout is not the cause of 11m13s hook SIGPIPE |
| `< 1200s` AND no `still-alive` | Server dropped before 20 min | **CONFIRMED** — Pantheon-side idle drop is plausibly the SIGPIPE trigger |
| Auth failure | Key/network problem | **INCONCLUSIVE** — re-check SSH credentials |

Compare elapsed-to-disconnect against the 11m13s gate runtime documented in
CONTEXT.md §Background and the original incident report.

**No partial evidence available from this run — H2 has not been touched.**

## Hypothesis #3 — EXIT-Trap

**Verdict:** **INCONCLUSIVE-INFRASTRUCTURE** across all three variants
(same blocking-env reason as H1: vc_pure failed before the trap mattered).

**Spike script:** `.planning/phases/03.1-publish-pipeline-audit-hardening/scratch/spike-exit-trap.sh`
**Driver logs:**

| Variant | Log file | Exit |
|---------|----------|------|
| A — omit `publish_gates_install_summary_trap` | `/tmp/spike-exit-trap-A-1778530771.log` | 1 |
| B — override trap body with `</dev/null` | `/tmp/spike-exit-trap-B-1778530778.log` | 1 |
| C — `trap - EXIT` before final PASSED echo | `/tmp/spike-exit-trap-C-1778530787.log` | 1 |

**Per-variant verdict:**

| Variant | Observation | Per-variant verdict |
|---------|-------------|---------------------|
| A | Hook ran without trap install; gate_vc_pure failed (missing phpunit binary, same as H1) | INCONCLUSIVE — exit 1, not 141 |
| B | Hook ran with trap body modified to redirect summary stdin from /dev/null; gate_vc_pure failed for the same reason | INCONCLUSIVE — exit 1, not 141 |
| C | Hook ran with `trap - EXIT` injected before PASSED; gate_vc_pure failed for the same reason | INCONCLUSIVE — exit 1, not 141 |

**Aggregate H3 verdict: INCONCLUSIVE-INFRASTRUCTURE.**

**What CAN be concluded:**

- None of the three variants introduced a regression observable in the short
  gates that did run. The trap-rework patterns (omit, isolate stdin, remove
  before PASSED) are all syntactically safe substitutions.
- The EXIT trap is installed by `publish_gates_install_summary_trap` at
  `scripts/ci/publish-gates.lib.sh:76-78`. The body invokes
  `bash ".../publish-failure-summary.sh" "$gate_exit"; exit "$gate_exit"`
  WITHOUT `</dev/null`, which RESEARCH §Pitfall 3 identifies as a minimal-blast
  fix candidate. The fix is non-breaking per Variant B execution.

**What CANNOT be concluded:**

- Whether the trap actually contributes to SIGPIPE on a successful long-gate
  run, because no variant reached the PASSED branch (all three failed earlier
  at vc_pure).

**Required follow-up for Wave 2 plan 03:**

Combine H3 Variant B's minimal patch (`</dev/null` in the trap body) with H1's
patches in a single sentinel-commit real-push test. If real-push exits 0 with
both patches applied and exits 141 with either patch reverted, root cause is
located and minimal blast radius is established.

## Hypothesis #4 — Network-Side

**Verdict:** **DEFERRED — requires operator sudo for tcpdump.**

Per plan 03.1-02 §"Hypothesis Ranking" RANK 4 cost-gate, H4 runs only if all of
H1/H2/H3 returned FALSIFIED or INCONCLUSIVE. Current state:

- H1: INCONCLUSIVE-INFRASTRUCTURE
- H2: DEFERRED
- H3: INCONCLUSIVE-INFRASTRUCTURE

So the cost-gate technically permits H4. But the operator-locked decision says
defer H4 entirely because sudo is not available without interactive
authorization, and the operator may choose to short-circuit to the
recommended Wave 2 fix shape (which addresses H1+H3 simultaneously) before
spending tcpdump capture effort.

**Operator instructions if H4 is desired:**

```bash
# Terminal A (root)
sudo tcpdump -i any -w /tmp/spike-pantheon.pcap \
  "host codeserver.dev.0bbb0799-c3de-441d-8d26-5caed25eba3f.drush.in" &
TCPDUMP_PID=$!

# Terminal B (your user, from a normal master checkout)
GIT_SSH_COMMAND="ssh -vvv" git push origin master 2>/tmp/spike-ssh-vvv.log

# Terminal A
sudo kill $TCPDUMP_PID

# Analyze: who sent FIN/RST first?
tshark -r /tmp/spike-pantheon.pcap -Y 'tcp.flags.fin == 1 || tcp.flags.reset == 1' \
  -T fields -e frame.time_relative -e ip.src -e ip.dst -e tcp.flags
```

**Verdict interpretation:**

| Observation | Verdict |
|-------------|---------|
| Pantheon IP (or intermediate) sent FIN/RST first while hook still running | CONFIRMED — escalate to Pantheon support; client-side cannot fix |
| Local IP sent FIN/RST first | FALSIFIED — bug is client-side; H1+H3 fix shape applies |
| Both sides sent FIN within a few ms (race) | INCONCLUSIVE — possibly a TCP keepalive issue |

## Recommended Fix Shape

**This section is the authoritative input for Wave 2 plan 03's `<action>` block.
Wave 2 plan 03 cites it by `§Recommended Fix Shape` heading.**

### Citation chain

- **H1 partial evidence:** The `</dev/null` patch at gate call sites and `exec
  0<&-` after PUSH_LINES are both NON-BREAKING per `/tmp/spike-fd-trace-1778530679.log`.
- **H3 partial evidence:** Adding `</dev/null` to the EXIT trap body
  (`publish-gates.lib.sh:77`) is NON-BREAKING per `/tmp/spike-exit-trap-B-1778530778.log`.
- **Aggregate:** All three minimal-blast patches are safe to apply together.
  Real-push validation (sentinel commit) is the remaining gap; Wave 2 plan 03
  Task 4 owns it.
- **Cited RESEARCH sections:** §Pitfall 1 (ddev exec stdin), §Pitfall 3 (EXIT
  trap stdin), §Pitfall 4 (`exec 0<&-` after PUSH_LINES read), §"Minimal SIGPIPE
  fix (FD isolation pattern)" lines 795-820.

### Concrete shell changes

**File 1: `scripts/ci/pre-push-strict.sh`**

Add `</dev/null` to every gate call (5 sites). Patch shape (line numbers refer
to current pre-push-strict.sh at sha256 `a8196d23759e3e323e6e3d9a40aab9f55eaf0ac07d59d1a7059955b7fda78137`):

```diff
@@ pre-push-strict.sh:43 (after the PUSH_LINES while-loop) @@
 while IFS=' ' read -r local_ref local_oid remote_ref remote_oid; do
   [[ -z "${local_ref:-}" ]] && continue
   PUSH_LINES+=("${local_ref}|${local_oid}|${remote_ref}|${remote_oid}")
 done
+exec 0<&-   # Detach stdin in parent so gate children cannot inherit
+            # git's pre-push pipe. See .planning/phases/03.1-.../03.1-02-SPIKE.md
+            # §Recommended Fix Shape.
```

```diff
@@ pre-push-strict.sh:199-207 (gate call block) @@
 cd "$REPO_ROOT"

 # Test gates — delegated to the shared library.
 # Local parity entry point: npm run gate:publish-local.
-gate_composer_dryrun
-gate_vc_pure
-gate_module_quality
+gate_composer_dryrun </dev/null
+gate_vc_pure </dev/null
+gate_module_quality </dev/null

 if [[ "$DEPLOY_BOUND_PROMPTFOO" == "true" ]]; then
-  gate_promptfoo_deploy_bound "$PROMPTFOO_BRANCH" "$DDEV_ASSISTANT_URL"
+  gate_promptfoo_deploy_bound "$PROMPTFOO_BRANCH" "$DDEV_ASSISTANT_URL" </dev/null
 else
-  gate_promptfoo_branch_aware "$PROMPTFOO_BRANCH"
+  gate_promptfoo_branch_aware "$PROMPTFOO_BRANCH" </dev/null
 fi
```

**File 2: `scripts/ci/publish-gates.lib.sh`**

Add `</dev/null` to the trap body so the publish-failure-summary subprocess
cannot inherit a stale stdin reference at hook EXIT time:

```diff
@@ publish-gates.lib.sh:76-78 @@
 publish_gates_install_summary_trap() {
-  trap 'gate_exit=$?; bash "'"$_PUBLISH_GATES_LIB_DIR"'/publish-failure-summary.sh" "$gate_exit"; exit "$gate_exit"' EXIT
+  trap 'gate_exit=$?; bash "'"$_PUBLISH_GATES_LIB_DIR"'/publish-failure-summary.sh" "$gate_exit" </dev/null; exit "$gate_exit"' EXIT
 }
```

### Why minimal-blast `</dev/null` over structural `exec 0<&-` alone

The plan-author identifies two candidate fixes (RESEARCH §"Minimal SIGPIPE fix"):
the per-gate `</dev/null` and the single parent-side `exec 0<&-`. This spike
recommends **both**, in that order:

1. **`exec 0<&-` after PUSH_LINES `done`** — single structural barrier; closes
   the parent's stdin once the hook has consumed git's push-refs protocol.
   Single source of truth; defends against any future gate added without
   `</dev/null`.
2. **`</dev/null` at each of the 5 gate calls** — belt-and-suspenders. If a
   future maintainer accidentally re-opens stdin in the parent (e.g., a
   `read -p` prompt added before the gates), the gate-level redirects still
   isolate each child.
3. **`</dev/null` in the EXIT trap body** — independent fix for the trap-side
   leak (RESEARCH §Pitfall 3). Even if the gate-level fixes catch every leak,
   the trap-side patch is cheap, non-breaking, and addresses a separate
   potential SIGPIPE site.

### Wave 2 plan 03 task seed

```
File 1: scripts/ci/pre-push-strict.sh
  - After line 43 (PUSH_LINES `done`): insert `exec 0<&-` + 2-line comment.
  - Line 199: `gate_composer_dryrun </dev/null`
  - Line 200: `gate_vc_pure </dev/null`
  - Line 201: `gate_module_quality </dev/null`
  - Line 204: `gate_promptfoo_deploy_bound "$PROMPTFOO_BRANCH" "$DDEV_ASSISTANT_URL" </dev/null`
  - Line 206: `gate_promptfoo_branch_aware "$PROMPTFOO_BRANCH" </dev/null`

File 2: scripts/ci/publish-gates.lib.sh
  - Line 77: append ` </dev/null` to the `bash ... publish-failure-summary.sh "$gate_exit"` invocation inside the trap body.

Verification:
  1. `bash scripts/ci/install-pre-push-strict-hook.sh` to copy the patched source into .git/hooks/pre-push.
  2. From a master checkout with composer install completed:
     - Create sentinel commit: `echo "test-$(date -u +%s)" >> .planning/test-bed/sentinel.txt && git add .planning/test-bed/sentinel.txt && git commit -m "test-bed: sentinel"`
     - Run: `git push origin master` (NOT --dry-run; the pack-send phase is the suspect)
     - Expected: exit 0, hook prints "Strict pre-push gate PASSED.", Pantheon advances.
     - Revert: `git revert --no-edit HEAD && git push origin master && git push github master`
  3. If exit 141 still observed: H2 (SSH idle) is the next suspect; run the
     deferred ssh-idle spike.
```

## Self-check

| Check | Result |
|-------|--------|
| `.git/hooks/pre-push` byte-identical to `scripts/ci/pre-push-strict.sh` post-spike | PASS (sha256 `a8196d2...`) |
| `git diff --name-only HEAD scripts/` empty | PASS |
| Three scratch spike scripts exist | PASS (`scratch/spike-fd-isolation.sh`, `scratch/spike-ssh-idle.sh`, `scratch/spike-exit-trap.sh`) |
| All four `## Hypothesis #N` sections present with explicit verdict | PASS (H1 INCONCLUSIVE-INFRASTRUCTURE, H2 DEFERRED, H3 INCONCLUSIVE-INFRASTRUCTURE, H4 DEFERRED) |
| `## Recommended Fix Shape` cites file:line targets | PASS (pre-push-strict.sh:43, :199, :200, :201, :204, :206; publish-gates.lib.sh:77) |
| No real push attempted | PASS (only `git push --dry-run -v origin master`) |
| No `--no-verify` invoked | PASS |
| No `git stash` invoked | PASS |

## AWAITING OPERATOR APPROVAL

This artifact is complete and ready for operator review per Plan 03.1-02 Task 4.
The orchestrator should route this SPIKE.md to the user for the `approved` /
`needs more spike` decision before Wave 2 fix plans run.
