---
phase: 03.1
slug: publish-pipeline-audit-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-11
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> This phase is CI/build orchestration — primary test layers are shell-trace, exit-code, and four-path matrix integration runs.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | bash + `bats-core` (Wave 0 installs); shell-trace + exit-code assertions; existing PHPUnit/promptfoo gates exercised as integration |
| **Config file** | `scripts/ci/gates.lock.json` (Wave 0 creates) — gate manifest for parity check |
| **Quick run command** | `bats tests/ci/gates-parity.bats` (per-task fast feedback after gate changes) |
| **Full suite command** | `bash scripts/ci/publish-gate-local.sh && bats tests/ci/` (full local-mirror + bats suite) |
| **Estimated runtime** | ~60s for bats quick run; ~11min for full local-mirror with all gates |

---

## Sampling Rate

- **After every task commit:** Run `bats tests/ci/gates-parity.bats` (or relevant sub-suite)
- **After every plan wave:** Run `bash scripts/ci/publish-gate-local.sh && bats tests/ci/`
- **Before `/gsd-verify-work`:** Four-path matrix executed against real `github` + Pantheon remotes (D-TESTBED-01)
- **Max feedback latency:** 60s (per-task); 11min (full suite per wave)

---

## Per-Task Verification Map

> Populated by the planner during plan creation. Each task in every PLAN.md must map here with its automated command. Wave 0 entries are created first so subsequent waves have somewhere to land their assertions.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-00-01 | 00 | 0 | PIPE-* | — | bats-core + shellcheck available | infra | `command -v bats && command -v shellcheck` | ❌ W0 | ⬜ pending |
| 03.1-00-02 | 00 | 0 | PIPE-* | — | gates.lock.json manifest defined | infra | `test -f scripts/ci/gates.lock.json && jq . scripts/ci/gates.lock.json` | ❌ W0 | ⬜ pending |
| 03.1-00-03 | 00 | 0 | PIPE-* | — | four-path test bed harness exists | infra | `test -x tests/ci/four-path-matrix.sh` | ❌ W0 | ⬜ pending |
| {N}-NN-NN | NN | N | PIPE-NN | T-03.1-NN | … | unit/integration/e2e | `{command}` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> **Planner directive:** populate the rest of this table from the per-plan task list. Every task must have either an automated command OR a Wave 0 dependency listed under "Wave 0 Requirements" below. No more than 3 consecutive tasks without an automated verify (sampling continuity rule).

---

## Wave 0 Requirements

- [ ] `bats-core` installed (host or DDEV; planner picks based on hypothesis spike outcome)
- [ ] `shellcheck` installed (host) — for shell-script static analysis
- [ ] `jq` available (verify; install if absent) — for `gates.lock.json` parsing
- [ ] `scripts/ci/gates.lock.json` created — canonical gate manifest (gate id, runner, expected exit-code behavior, scope: pre-push|local-mirror|both)
- [ ] `tests/ci/four-path-matrix.sh` harness exists — sentinel-file + git-revert cleanup pattern per RESEARCH.md
- [ ] `tests/ci/gates-parity.bats` exists — verifies `publish-gate-local.sh` invokes the same gate IDs as `pre-push-strict.sh` per the lockfile
- [ ] `tests/ci/sigpipe-repro.sh` exists — minimal reproducer for the SIGPIPE defect, used by the FD-isolation spike
- [ ] `tests/ci/dual-remote-invariant.sh` exists — asserts `github` and `origin`/Pantheon agree on master HEAD after `git:finish`

*Wave 0 must complete before any Wave 1 task touches `pre-push-strict.sh`, `publish-gates.lib.sh`, or `publish-gate-local.sh` — otherwise fixes cannot be verified without manual `git push` attempts.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `git push origin master` (no `--no-verify`) succeeds end-to-end with non-trivial diff | PIPE-01 (SIGPIPE fix) | Requires a real protected-master push event; cannot be safely automated without disturbing branch history | Cherry-pick a no-op commit onto a sentinel branch fast-forwarded to master, push, observe exit code 0 and matching `github`+`origin` HEAD; revert sentinel commit |
| Pantheon SSH key/agent timeout behavior over an 11-minute gate run | PIPE-* (dual-remote) | Pantheon SSH lifecycle is environmental; harness can detect timeout but cannot cause it deterministically | Run full gate suite with `SSH_AUTH_SOCK` audit; verify Pantheon push step still authenticates after gate phase |
| Branch protection enforcement on GitHub side | PIPE-* | GitHub-side protection cannot be tested without a real push attempt to a protected ref | Attempt `git push --force` to master from a fresh checkout; verify GitHub rejects |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (bats, shellcheck, gates.lock.json, four-path harness, parity bats, sigpipe-repro, dual-remote-invariant)
- [ ] No watch-mode flags (CI/pre-push gates must be one-shot)
- [ ] Feedback latency < 60s for quick run; < 11min for full
- [ ] `nyquist_compliant: true` set in frontmatter once planner completes the Per-Task Verification Map

**Approval:** pending

---

## Tooling Audit (Wave 0 Result)

**Audit timestamp:** 2026-05-11T00:00:00Z (Plan 03.1-01 Task 1 execution)
**Host:** /home/evancurry/idaho-legal-aid-services (WSL2 / Ubuntu)

| Tool       | `command -v` result | Version | Install path chosen |
|------------|---------------------|---------|---------------------|
| `bats`     | NOT FOUND           | n/a     | Fallback: hand-rolled bash (per RESEARCH §A5 / §"Environment Availability"). Non-interactive sudo unavailable, so apt install requires operator action. `tests/ci/gates-parity.bats` is created as a bats-style file with a single failing-stub test; runners may execute it via `bats` once installed, or via `bash` fallback per the script's shebang+failure semantics. |
| `shellcheck` | NOT FOUND         | n/a     | Fallback: manual review (per RESEARCH "Missing dependencies with fallback"). Non-interactive sudo unavailable. Plan acceptance criteria for shellcheck-clean files are MET BY CONSTRUCTION (canonical header, `set -euo pipefail`, double-quoted expansions); the gate becomes runnable once `shellcheck` is installed. Operator action: `sudo apt-get install -y shellcheck`. |
| `jq`       | /usr/bin/jq         | jq-1.7  | Already installed. Used by `scripts/ci/gates.lock.json` consumers in Wave 3. |

**Rationale for hand-rolled fallback (per RESEARCH A5):** bats-core would require either (a) apt install with sudo, (b) vendored git submodule under `vendor/bats-core/` adding maintenance overhead, or (c) a per-developer pin. The hand-rolled fallback uses `tests/ci/_assert.sh` (created in Task 3) with `assert_eq`, `assert_command_succeeds`, `assert_grep`, `assert_not_grep` helpers mirroring `scripts/git/common.sh:11-25` logger shape. This is sufficient for ~10 test cases planned in this phase. If/when bats is installed, the `.bats` file remains valid for native execution; the other harnesses (`.sh`) are bats-agnostic.

**Operator install commands (for future activation, not blocking this plan):**
```bash
sudo apt-get update
sudo apt-get install -y bats shellcheck
```

**Wave 0 unblocking decision:** Plan proceeds with hand-rolled fallback. `jq` is sufficient for the lockfile-parity verifier planned in Wave 3.
