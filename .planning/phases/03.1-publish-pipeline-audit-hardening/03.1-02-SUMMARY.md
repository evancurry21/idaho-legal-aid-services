---
phase: 03.1
plan: 02
subsystem: publish-pipeline / ci-hardening / sigpipe-spike
tags: [spike, sigpipe, falsification, fd-isolation, dry-run-only]
requires: [03.1-01]
provides: [03.1-02-SPIKE.md]
affects: []
key-files:
  created:
    - .planning/phases/03.1-publish-pipeline-audit-hardening/03.1-02-SPIKE.md
    - .planning/phases/03.1-publish-pipeline-audit-hardening/scratch/spike-fd-isolation.sh
    - .planning/phases/03.1-publish-pipeline-audit-hardening/scratch/spike-ssh-idle.sh
    - .planning/phases/03.1-publish-pipeline-audit-hardening/scratch/spike-exit-trap.sh
  modified: []
decisions:
  - "Hypothesis #1 (FD-inheritance): INCONCLUSIVE-INFRASTRUCTURE — patches are non-breaking but suspect gates (module_quality, promptfoo) never ran because vc_pure failed earlier (missing vendor/bin/phpunit in worktree)."
  - "Hypothesis #2 (SSH idle timeout): DEFERRED — operator-driven 1200s SSH session required; script staged at scratch/spike-ssh-idle.sh."
  - "Hypothesis #3 (EXIT-trap): INCONCLUSIVE-INFRASTRUCTURE across all 3 variants — same blocking env condition as H1; trap-rework patterns proven non-breaking."
  - "Hypothesis #4 (Network-side): DEFERRED — requires interactive sudo for tcpdump."
  - "Recommended Fix Shape: combine `</dev/null` at 5 gate call sites + `exec 0<&-` after PUSH_LINES read + `</dev/null` in EXIT trap body. Real-push validation deferred to Wave 2 plan 03 Task 4 sentinel-commit pattern."
metrics:
  duration: "~3 min driver runtime + ~10 min agent overhead"
  completed: 2026-05-11
push_mode: dry-run-only
hooks_restored: true
production_scripts_modified: 0
---

# Phase 03.1 Plan 02: Wave 1 SIGPIPE Spike Summary

**One-liner:** Empirical SIGPIPE-141 spike across four root-cause hypotheses
via `git push --dry-run -v origin master` only; H1 + H3 returned
INCONCLUSIVE-INFRASTRUCTURE (worktree missing `vendor/bin/phpunit` blocked
the suspect long-runtime gates from running); H2 + H4 deferred per
operator decision; a Recommended Fix Shape combining `</dev/null` at gate
call sites, `exec 0<&-` after PUSH_LINES, and `</dev/null` in the EXIT trap
is locked for Wave 2 plan 03 with real-push validation as the closing step.

**Root cause located:** No — spike is INCONCLUSIVE under the operator's
dry-run-only constraint and the worktree's incomplete composer install. The
Recommended Fix Shape is justified by non-breakage evidence on the short gates
that did run plus the established Pitfalls 1/3/4 documented in
`03.1-RESEARCH.md`. Wave 2 plan 03 Task 4's sentinel-commit real-push test is
the remaining empirical step.

**Recommended fix shape:** Apply three patches together —
`</dev/null` at each of the 5 gate call sites in
`scripts/ci/pre-push-strict.sh:199-207`; `exec 0<&-` immediately after the
PUSH_LINES `done` line at `scripts/ci/pre-push-strict.sh:43`; `</dev/null` in
the trap body at `scripts/ci/publish-gates.lib.sh:77`. All three patches were
proven non-breaking by the spike's variant runs (composer dry-run, awk-applied
gate-call redirects, three EXIT-trap variants). The combination follows the
RESEARCH-documented Pitfalls 1, 3, and 4 minimal-blast pattern.

**Evidence trail:** See
`.planning/phases/03.1-publish-pipeline-audit-hardening/03.1-02-SPIKE.md`
(authoritative; every verdict references its driver log under `/tmp/`).

## Tasks Completed

| Task | Name | Status | Artifact |
|------|------|--------|----------|
| 1 | Hypothesis #1 FD-isolation spike | RAN; verdict INCONCLUSIVE-INFRASTRUCTURE | `scratch/spike-fd-isolation.sh` + `/tmp/spike-fd-trace-1778530679.log` |
| 2 | Hypotheses #2 and #3 conditional spikes | H2 DEFERRED (script staged); H3 RAN 3-variant; all INCONCLUSIVE-INFRASTRUCTURE | `scratch/spike-ssh-idle.sh`, `scratch/spike-exit-trap.sh` + `/tmp/spike-exit-trap-{A,B,C}-*.log` |
| 3 | H4 disposition + Recommended Fix Shape | H4 DEFERRED; Recommended Fix Shape locked with file:line citations | SPIKE.md §Recommended Fix Shape |
| 4 | Checkpoint:human-verify (operator approval) | NOT EXECUTED — preserved for orchestrator-user routing | — |

## Deviations from Plan

### Auto-fixed Issues

**None.** The plan executed within the operator-locked constraints exactly as
specified. The INCONCLUSIVE verdicts are honest empirical findings, not
deviations.

### Operator-locked decisions honored

1. Hypothesis #1 push mode: dry-run only. No real-push attempted.
2. Hypothesis #2: deferred-to-operator; spike script staged but not executed.
3. Hypothesis #4: deferred-to-operator; no sudo invoked.
4. Stopped at Task 4 (checkpoint preserved).
5. No `git push --no-verify` invoked.
6. No `git stash` invoked.

### Spike-environment observations (informational, not deviations)

- The worktree at `agent-a6932865f5ec4282c` does not have a populated
  `vendor/` directory. `composer dry-run` simulated 326 installs successfully,
  but `vendor/bin/phpunit` does not exist, so `gate_vc_pure` aborts before the
  long-runtime gates run. Wave 2 plan 03's real-push validation must do
  `composer install` in its worktree first.
- The worktree's HEAD is on `worktree-agent-a6932865f5ec4282c` (per the
  parallel-executor safety contract), not on `master`. The hook's master-only
  protection blocks (`pre-push-strict.sh:148-171`) are therefore skipped during
  this spike. Wave 2 plan 03's real-push validation should run from a clean
  master checkout to exercise those blocks.
- Local `master` (`d46199fd4`) is behind `origin/master` (`02ec4f35e`). Git
  rejected the dry-run as non-fast-forward AFTER the hook completed — this
  rejection is independent of the hook's pass/fail and was visible at the tail
  of every dry-run log.

## Threat Flags

None. No new security-relevant surface was introduced by this spike. The
temporary hook replacement was trap-restored; sha256 of `.git/hooks/pre-push`
matches `scripts/ci/pre-push-strict.sh` post-spike.

## Self-Check: PASSED

- `.planning/phases/03.1-publish-pipeline-audit-hardening/03.1-02-SPIKE.md` exists with all 4 `## Hypothesis #N` sections + `## Recommended Fix Shape`
- All three scratch spike scripts exist and are executable (`scratch/spike-fd-isolation.sh`, `scratch/spike-ssh-idle.sh`, `scratch/spike-exit-trap.sh`)
- `.git/hooks/pre-push` byte-identical to `scripts/ci/pre-push-strict.sh` (sha256 `a8196d23759e3e323e6e3d9a40aab9f55eaf0ac07d59d1a7059955b7fda78137`)
- `git diff --name-only HEAD scripts/` empty (no production scripts modified)
- No `git push --no-verify` invoked anywhere
- No `git stash` invoked anywhere
- Checkpoint Task 4 preserved (not auto-approved)

## AWAITING OPERATOR APPROVAL

This plan is at checkpoint:human-verify (Task 4). The orchestrator should
route SPIKE.md to the operator for review. The operator types `approved` to
proceed to Wave 2, or `needs more spike` to re-run plan 02 with adjusted
constraints (e.g., real-push enabled, or H2/H4 re-enabled).
